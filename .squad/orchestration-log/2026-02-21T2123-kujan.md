# Orchestration: Kujan — 2026-02-21T21:23:00Z

**Agent:** Kujan (SDK Expert)  
**Mode:** background  
**Model:** claude-sonnet-4.5  
**Task:** M0 blocker audit (#190, #193, #194)  
**Duration:** ~20 min  

## Outcome

M0 blocker is **already unblocked** — SDK dependency can be swapped immediately.

### Key Findings

1. **SDK is published on npm** — `@github/copilot-sdk@0.1.25` (28 versions available, SLSA attestations)

2. **Squad's SDK surface is minimal** — Only 1 runtime import: `CopilotClient` in `src/adapter/client.ts`

3. **File reference (`file:../copilot-sdk/nodejs`) is the only M0 blocker** — Prevents npm publish and breaks CI without sibling directory

4. **Recommended fix:** One-line change to `optionalDependencies`:  
   ```json
   "@github/copilot-sdk": "^0.1.25"
   ```

5. **Verified:** Build passes (0 errors), all 1592 tests pass with npm reference

### Technical Details

- **Build:** ✅ `tsc --noEmit` with 0 errors
- **Tests:** ✅ 1592/1592 pass (49 test files)
- **Install:** ✅ Resolves cleanly (adds 4 packages)
- **SDK stays in `optionalDependencies`** — Preserves zero-dependency scaffolding guarantee (decision by Rabin)

## Deliverables

- Written `.squad/decisions/inbox/kujan-sdk-m0-audit.md`
- Commented on GitHub issues #190, #193, #194

## Next Steps

- Merge audit findings to shared decisions.md (Scribe)
- Update Edie's history.md (SDK dependency can be swapped)
