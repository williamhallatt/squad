# Fenster: OTel Phase 2 — Tool Traces & Agent Metrics

**Date:** 2026-02-22T09:33:00Z  
**Issues:** #260, #262  
**Status:** ✅ SUCCESS

## Work Done

- Enhanced `defineTool()` with optional `agentName` config parameter — recorded as `agent.name` span attribute
- Implemented `sanitizeArgs()` helper that strips fields matching `/token|secret|password|key|auth/i` from span attributes
- Truncates sanitized args to 1024 chars
- Added `result.length` attribute to `squad.tool.result` events
- Wired `recordAgentSpawn`, `recordAgentDuration`, `recordAgentError`, `recordAgentDestroy` metrics into both AgentSessionManager and AgentLifecycleManager
- Duration computed from `createdAt` in destroy path

## Key Decisions

1. **Sanitization pattern:** Field-name matching (not value inspection) for speed and predictability
2. **Dual wiring:** Agent metrics wired at both SessionManager and LifecycleManager because they can be used independently
3. **Parent span propagation deferred:** TODO comment added — will wire when agent.work span lifecycle is complete

## Outcome

Consistent instrumentation patterns across tool and agent telemetry. Metrics ready for Phase 2 validation. Parent span propagation tracked for future work.

## Commit

(included in Phase 2 rollup)
