# Edie: CLI Entry Point Split (2026-02-22T02:07:14Z)

**Status:** ✅ COMPLETE

**Outcome:**
- `src/index.ts` is pure barrel export (zero side effects)
- `src/cli-entry.ts` contains full `main()` function
- Closes #187
- Enables library-safe consumption for SquadUI

**Decision:**
- `VERSION` exported from `index.ts` (public API)
- `SquadError` explicitly exported from barrel
- `build:cli` script already references `dist/cli-entry.js`

**Tests:** All 1683 passing

**Reference:** `.squad/decisions.md` → CLI entry point split
