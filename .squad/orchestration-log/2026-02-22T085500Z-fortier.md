# Fortier — 2026-02-22T08:55:00Z

## Phase: OTel Foundation (Phase 1)

**Issues:** #255, #256

**Outcome:** SUCCESS

### Work Done
- Created `packages/squad-sdk/src/runtime/otel.ts` — Provider initialization with `NodeSDK` and OTLP exporters
- Created `packages/squad-sdk/src/runtime/otel-bridge.ts` — Bridge from TelemetryCollector to OTel spans
- Updated `packages/squad-sdk/src/index.ts` with barrel exports
- Updated `packages/squad-sdk/package.json` with OTel subpath exports

### Key Decision
**OTel Foundation — NodeSDK over individual providers:** Use `@opentelemetry/sdk-node`'s NodeSDK class as single provider manager. Import Resource and PeriodicExportingMetricReader from its re-exports.

### Consequences
- Single `_sdk.shutdown()` handles both tracing and metrics cleanup
- No version skew between trace and metric providers
- `getTracer()` / `getMeter()` return no-op when OTel not initialized — zero overhead by default
- Bridge is additive; can be registered alongside existing transports

### Next
Fenster instruments AgentSessionManager, AgentLifecycleManager, Coordinator, SquadCoordinator with OTel spans.
