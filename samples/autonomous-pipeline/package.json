{
  "name": "@squad-samples/autonomous-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Showcase demo — autonomous multi-agent pipeline with live terminal dashboard, cost tracking, telemetry, and OTel observability",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@bradygaster/squad-sdk": "^0.8.0",
    "chalk": "^5.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
